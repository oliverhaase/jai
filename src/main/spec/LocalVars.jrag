
aspect LocalVars {
	
	
	syn LocalVars Method.createInitialLocalVars() {
		LocalVars result = new LocalVars(getMaxLocals());

		int index = 0;		
		for ( Type arg : getArgs() ) 
			if ( arg instanceof PrimitiveType ) 
				index += ((PrimitiveType) arg).getSize();
			else 
				result = result.set(index++, arg);		
		
		return result;
	}
	
	syn LocalVars Instruction.transferLocalVars(LocalVars localVarsIn, Type slot) = localVarsIn;
	
	eq StoreInstruction.transferLocalVars(LocalVars localVarsIn, Type slot) 
		= localVarsIn.set(getIndex(), slot);	
	
	
}